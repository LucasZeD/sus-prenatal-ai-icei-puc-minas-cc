# Minimal deps for offline service tests (no PyTorch/pyannote).
# Tests exercise the `fallback` backend (pure numpy) and the HTTP contract.
fastapi>=0.115.0
python-multipart>=0.0.12
numpy>=1.26.0
scipy>=1.11.0
httpx>=0.27.0
pytest>=8.0.0
