# Frontend (Vite + React)

Instruções de ambiente, Docker, Prisma, testes e variáveis de ambiente estão no guia único do repositório:

**[../README.md](../README.md)**

Resumo rápido: copie `frontend/.env.example` para `frontend/.env`, defina `VITE_API_BASE_URL`, depois `npm install` e `npm run dev` a partir desta pasta.
